# Middleware Directory

This directory will store custom Express middleware functions.

### Planned Middleware:
- **`authMiddleware.js`**: Protects private routes by verifying JWT tokens sent in the `Authorization: Bearer <token>` header.
- **`errorHandler.js`**: Centralized error-handling middleware to catch and format API errors cleanly.
